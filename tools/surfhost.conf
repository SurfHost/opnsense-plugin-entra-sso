# SurfHost OPNsense plugin repository
#
# This is the source copy. The file users fetch is served from the gh-pages
# branch at https://surfhost.github.io/opnsense-plugin-entra-sso/surfhost.conf
# Keep the two in sync when changing anything here.
#
# Install on OPNsense:
#   fetch -o /usr/local/etc/pkg/repos/surfhost.conf \
#       https://surfhost.github.io/opnsense-plugin-entra-sso/surfhost.conf
#   pkg update
#
# Remove again:
#   rm /usr/local/etc/pkg/repos/surfhost.conf && pkg update

surfhost: {
  url: "https://surfhost.github.io/opnsense-plugin-entra-sso/${ABI}",
  priority: 5,
  enabled: yes
}
